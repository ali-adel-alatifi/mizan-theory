"""
==========================================================================
🌌 قمرة القيادة المركزية – المنصة الكونية الكاملة (الإصدار النهائي)
==========================================================================
تجمع كل المراحل: المستشار، لوحة القيادة، مختبر المجتمع، وصراع الحضارات.
تم تعديل دالة المحاكاة لتوضيح "فجوة الاستدراج" بشكل أسرع.
==========================================================================
"""

import streamlit as st
import numpy as np
import matplotlib.pyplot as plt
import time
import random

# =============================================
# إعداد الصفحة
# =============================================
st.set_page_config(page_title="قمرة القيادة المركزية", page_icon="🌌", layout="wide")
st.title("🌌 قمرة القيادة المركزية – المنصة الكونية الكاملة")
st.markdown("### تجمع كل وحدات المنصة في مكان واحد: المستشار، لوحة القيادة، مختبر المجتمع، وصراع الحضارات")

# =============================================
# دالة المحاكاة المعدلة (لتسريع الاستدراج)
# =============================================
def run_standard_simulation(W0, B0, E0, years=200, lag=25):
    W = np.zeros(years); B = np.zeros(years); S = np.zeros(years); E = np.zeros(years)
    W[0], B[0], E[0] = W0, B0, E0; S[0] = W0 * B0
    for t in range(1, years):
        H = 10 / (S[t-1] + 0.1)
        # تم زيادة معامل الكسل (E) لتسريع انهيار W
        dW = (0.08 * H) - (0.05 * E[t-1]) - (0.04 * (1 - B[t-1]))
        # تم زيادة معامل الترف (E) لتسريع انهيار B
        dB = (-0.04 * E[t-1]) + (0.01 * (1 - B[t-1]) * W[t-1] * (1 - W[t-1]))
        W[t] = max(0.0, min(1.0, W[t-1] + dW))
        B[t] = max(0.0, min(1.0, B[t-1] + dB))
        S[t] = W[t] * B[t]
        past_idx = t - lag
        S_past = S[past_idx] if past_idx >= 0 else S[t]
        dE = 0.05 * (S_past - E[t-1])
        E[t] = max(0.0, min(1.0, E[t-1] + dE))
    return W, B, S, E

# =============================================
# الشريط الجانبي – قائمة الوحدات
# =============================================
st.sidebar.title("🧭 قائمة الوحدات")
module = st.sidebar.radio(
    "اختر الوحدة:",
    ["🏠 القمرة الرئيسية", "🧠 المستشار الدلالي", "🌍 لوحة القيادة الوطنية", "👥 مختبر المجتمع", "⚔️ صراع الحضارات"]
)

st.sidebar.markdown("---")
st.sidebar.markdown("*المنصة الكونية الكاملة – v6.1*")

# =============================================
# الوحدة 0: القمرة الرئيسية (نظرة عامة)
# =============================================
if module == "🏠 القمرة الرئيسية":
    st.header("🏠 مرحبًا بك في قمرة القيادة المركزية")
    st.markdown("""
    ### 🌌 المنصة الكونية الكاملة – نظرية الميزان
    
    هذه هي "غرفة التحكم" النهائية. من هنا، يمكنك الوصول إلى كل وحدات المنصة:
    
    1.  **🧠 المستشار الدلالي**: اسأل أي سؤال عن الحضارة والتاريخ والنفس، وستحصل على إجابة مع محاكاة حية.
    2.  **🌍 لوحة القيادة الوطنية**: أدخل بيانات أي دولة، لترى موقعها على خريطة الاستدراج وتوقعات مستقبلها.
    3.  **👥 مختبر المجتمع**: شاهد كيف يتفاعل 500 فرد في مجتمع واحد، وكيف ينتشر الخير والشر.
    4.  **⚔️ صراع الحضارات**: عالم افتراضي يضم 6 دول، تتحارب وتتحالف وتغزو بعضها ثقافيًا.
    
    ---
    ### 📊 معادلة اليوم: S = W × B
    
    **S** = الثبات الوجودي (الطمأنينة والعزة والتمكين).
    **W** = الولاء لله (الإيمان والعمل الصالح والعدل).
    **B** = البراءة من الطاغوت (الجهاد والعفة والاستقلال).
    
    إنها معادلة **ضربية**. إذا كان أحد طرفيها صفرًا، كان الثبات معدومًا.
    """)
    
    # رسم توضيحي سريع للمعادلة
    col1, col2, col3 = st.columns(3)
    with col1:
        W_demo = st.slider("W (الولاء)", 0.0, 1.0, 0.8, 0.05, key="demo_W")
    with col2:
        B_demo = st.slider("B (البراءة)", 0.0, 1.0, 0.8, 0.05, key="demo_B")
    with col3:
        S_demo = W_demo * B_demo
        st.metric("S (الثبات)", f"{S_demo:.2f}")
    
    W_s, B_s, S_s, E_s = run_standard_simulation(W_demo, B_demo, 0.3, years=150)
    fig, ax = plt.subplots(figsize=(10, 4))
    ax.plot(S_s, 'g-', linewidth=2, label='S (الثبات)')
    ax.plot(E_s, 'b--', linewidth=2, label='E (التمكين)')
    ax.set_title('منحنى توضيحي: دورة الحضارة')
    ax.legend(); ax.grid(True, alpha=0.3)
    st.pyplot(fig)
    # =============================================
# الوحدة 1: المستشار الدلالي (الخبير العام – الإصدار الثوري)
# =============================================
elif module == "🧠 المستشار الدلالي":
    st.header("🧠 المستشار الدلالي – الخبير العام")
    st.markdown("اسأل أي سؤال عن الحضارة، التاريخ، النفس، الفيزياء، أو أي شيء يتعلق بـ 'نظرية الميزان'.")

    # =============================================
    # 1. قاعدة المعرفة المنطقية (Rule-Based Knowledge)
    # =============================================
    KNOWLEDGE_RULES = [
        # [الموضوع، الكلمات المفتاحية، الإجابة]
        ["تعريف_المعادلة", ["ما", "معادلة", "S", "W", "B", "تعريف", "شرح", "الثبات", "الوجودي", "عرف", "اشرح"],
         "معادلة الثبات الوجودي هي S = W × B. حيث S هو 'الثبات' (الطمأنينة والعزة والتمكين)، وW هو 'الولاء لله' (الإيمان والعمل الصالح والعدل)، وB هو 'البراءة من الطاغوت' (الجهاد والعفة والاستقلال). إنها معادلة **ضربية**: إذا كان أحد الطرفين صفرًا، كان الثبات معدومًا. هذه المعادلة مستنبطة من قوله تعالى: {فَمَن يَكْفُرْ بِالطَّاغُوتِ وَيُؤْمِن بِاللَّهِ فَقَدِ اسْتَمْسَكَ بِالْعُرْوَةِ الْوُثْقَىٰ}."],
        
        ["الاستدراج", ["استدراج", "الاستدراج", "الإلهي", "ينهار", "فجأة", "غنى", "ترف", "أخذهم", "بغتة", "أبواب", "كل شيء", "نسوا", "فرحوا", "انهيار", "مفاجئ", "سقوط", "فجائي"],
         "الاستدراج الإلهي هو أن يُملي الله للظالمين، ويمدهم في طغيانهم، ويفتح عليهم أبواب الدنيا، حتى إذا فرحوا بما أوتوا، أخذهم بغتة. في النموذج الرياضي، يظهر عندما ينهار 'الثبات' (S) بينما 'التمكين المادي' (E) ما زال مرتفعًا. هذا يفسر لماذا تنهار الحضارات فجأة بعد قرون من القوة. **الاستدراج هو 'فخ إلهي'**: تعتقد أنك بخير لأنك غني وقوي، ولكنك في الحقيقة على شفا جرف هارٍ."],
        
        ["دورة_الحضارة", ["حضارة", "الحضارة", "تقوم", "تسقط", "تنهار", "دورة", "صعود", "سقوط", "مراحل", "ذروة", "تاريخ", "أمم", "دول", "إمبراطورية", "قيام", "سقوط"],
         "دورة الحضارة تمر بأربع مراحل: 1. **الصعود**: يرتفع W (الولاء) وB (البراءة)، ويبدأ التمكين (E) في الارتفاع ببطء. 2. **الذروة**: تستقر الحضارة في قوتها. 3. **الاستدراج**: يبدأ W وB في الانهيار بسبب الغنى والترف، ولكن E يظل مرتفعًا! الحضارة تبدو قوية لكنها تحتضر. 4. **الانهيار**: ينهار E فجأة ليلحق بـ S المنهارة. هذا قانون إلهي لا يحابي أحدًا."],
        
        ["ثبات_الفرد", ["فرد", "شخص", "نفس", "إنسان", "يثبت", "ثبات", "استقرار", "طمأنينة", "قلق", "ضيق", "كيف", "أنا", "شخصي", "أثبت", "أقوي", "نفسي"],
         "الفرد يثبت على الحق بتحقيق معادلة S = W × B في نفسه. **لزيادة W**: الصلاة، الزكاة، الصيام، الحج، الذكر، العلم. **لزيادة B**: جهاد النفس، العفة، الأمر بالمعروف، النهي عن المنكر. عندما يوالي العبد ربه (W) ويتبرأ من هواه (B)، يشعر بالطمأنينة. وعندما ينعكس الأمر، يشعر بالضيق والقلق. **التطبيق اليومي**: راجع نفسك كل ليلة: أين W وB لديّ اليوم؟"],
        
        ["القرآن_والعلم", ["فيزياء", "علم", "كون", "جاذبية", "تنافر", "مناعة", "كيمياء", "بيولوجيا", "ذرة", "خلية", "القرآن", "والعلم", "تطابق", "طبيعة", "قوانين"],
         "القرآن والفيزياء كتابان من مؤلف واحد. **'الجاذبية'** في الفيزياء هي 'الولاء' (W): قوة جذب بين الكتل. **'التنافر'** هو 'البراءة' (B): قوة تنافر بين الشحنات المتشابهة. **وجهاز المناعة** في الجسد يميز 'الذات' (W) عن 'اللا ذات' (B). هذه كلها تجليات لمعادلة S = W × B في الكون. **الخلاصة**: الإسلام ليس دينًا منفصلاً عن العلم، بل هو 'دين الكون' كله."],
        
        ["اقتصاد", ["اقتصاد", "مال", "ربا", "غش", "زكاة", "صدقة", "فقر", "غنى", "ثروة", "أزمة", "اقتصادية", "معيشة", "رزق"],
         "في 'الميزان'، الاقتصاد يسير بقوانين إلهية. **الزكاة والصدقات** تزيد W (الولاء) وتطهر المال. **الربا والغش** يزيدان 'الظلام' (B سالب) ويمحقان البركة. قال تعالى: {يَمْحَقُ اللَّهُ الرِّبَا وَيُرْبِي الصَّدَقَاتِ}. الاقتصاد الذي يقوم على الربا والتبعية (B=0) سينهار، حتى لو بدا قويًا. أما الاقتصاد القائم على التكافل (W=1) فهو مبارك."],
        
        ["سياسة_وحكم", ["سياسة", "حكم", "حاكم", "دولة", "ظلم", "عدل", "شورى", "استبداد", "قانون", "دستور", "حكومة", "قائد", "رئيس"],
         "في 'الميزان'، الحكم الرشيد هو الذي يحقق W=1 (العدل والشورى وتطبيق شرع الله) وB=1 (رفض القوانين الوضعية المخالفة والاستقلال عن الطواغيت). الحاكم الذي يظلم (W=0) أو يتبع أعداء الأمة (B=0) هو في 'منطقة الهلاك'. قال تعالى: {وَمَن لَّمْ يَحْكُم بِمَا أَنزَلَ اللَّهُ فَأُولَٰئِكَ هُمُ الْكَافِرُونَ}."],
        
        ["تربية", ["تربية", "تعليم", "جيل", "أطفال", "شباب", "مدرسة", "منهج", "أخلاق", "قيم", "نشء", "تنشئة"],
         "التربية في 'الميزان' هي عملية بناء 'الولاء' (W) و'البراءة' (B) في قلب الطفل. **تعليم W**: غرس حب الله ورسوله والمسلمين. **تعليم B**: تنمية المناعة الفكرية ضد الشبهات، والعفة ضد الشهوات. الجيل الذي يتربى على هذه المعادلة هو 'جيل النصر'."],
        
        ["توبة", ["توبة", "تائب", "ذنب", "معصية", "رجوع", "عودة", "استغفار", "ندم", "إقلاع"],
         "التوبة في 'الميزان' هي 'إعادة ضبط' للمعادلة. عندما يذنب العبد، تنخفض B لديه. التوبة النصوح (الندم + الإقلاع + العزم) هي 'قفزة' تصحيحية تعيد S إلى مسارها الصحيح. وهي متاحة دائمًا: {إِلَّا مَن تَابَ وَآمَنَ وَعَمِلَ عَمَلًا صَالِحًا فَأُولَٰئِكَ يُبَدِّلُ اللَّهُ سَيِّئَاتِهِمْ حَسَنَاتٍ}."],
    ]
    
    # =============================================
    # 2. محرك التحليل والاستنتاج (Inference Engine)
    # =============================================
    def analyze_and_respond(query):
        """يحلل السؤال ويختار أفضل إجابة وسيناريو."""
        q = query.lower()
        best_rule = None
        best_score = 0
        
        # البحث في القواعد
        for rule in KNOWLEDGE_RULES:
            score = 0
            for keyword in rule[1]:
                if keyword.lower() in q:
                    score += 3  # تطابق كامل
                # تطابق جزئي
                for qw in q.split():
                    if len(keyword) > 3 and len(qw) > 2:
                        if keyword.lower().startswith(qw.lower()) or qw.lower().startswith(keyword.lower()):
                            score += 1
            if score > best_score:
                best_score = score
                best_rule = rule
        
        # استخراج السيناريو (W0, B0, E0)
        W0, B0, E0 = 0.5, 0.5, 0.3  # افتراضي
        
        if any(w in q for w in ["استدراج", "غنى", "ترف", "فجأة", "ينهار", "سقوط", "انهيار"]):
            W0, B0, E0 = 0.3, 0.25, 0.9
        elif any(w in q for w in ["صاعد", "قوة", "إيمان", "بداية", "نهضة", "صحوة"]):
            W0, B0, E0 = 0.9, 0.9, 0.1
        elif any(w in q for w in ["فرد", "نفس", "شخص", "ثباتي", "أثبت", "شخصي", "توبة", "ذنب"]):
            W0, B0, E0 = 0.6, 0.5, 0.2
        elif any(w in q for w in ["ظلم", "حكم", "حاكم", "استبداد"]):
            W0, B0, E0 = 0.2, 0.7, 0.8
        elif any(w in q for w in ["ربا", "غش", "اقتصاد", "مال"]):
            W0, B0, E0 = 0.5, 0.2, 0.6
            
        return best_rule, W0, B0, E0, best_score
    
    # =============================================
    # 3. الواجهة
    # =============================================
    user_q = st.text_input("✍️ اسأل المستشار:", placeholder="مثال: كيف تنهار الحضارة؟ أو كيف أثبت على الحق؟ أو ما هو الاستدراج؟")
    
    if user_q:
        with st.spinner("🧠 المستشار يحلل سؤالك..."):
            best_rule, W0, B0, E0, score = analyze_and_respond(user_q)
            
            if best_rule and score > 1:
                # عرض الإجابة
                st.success(f"💡 **إجابة المستشار:** {best_rule[2]}")
                
                # عرض تحليل السيناريو
                st.info(f"📊 **تحليل السيناريو:** بناءً على سؤالك، قمت بتشغيل محاكاة بحالة ابتدائية: W={W0:.2f}, B={B0:.2f}, E={E0:.2f}")
                
                # تشغيل المحاكاة
                W_s, B_s, S_s, E_s = run_standard_simulation(W0, B0, E0)
                fig, ax = plt.subplots(figsize=(10, 5))
                ax.plot(S_s, 'g-', linewidth=2, label='S (الثبات)')
                ax.plot(E_s, 'b--', linewidth=2, label='E (التمكين)')
                max_S = np.argmax(S_s); max_E = np.argmax(E_s)
                if max_S < max_E:
                    ax.axvspan(max_S, max_E, alpha=0.2, color='red', label=f'منطقة الاستدراج ({max_E - max_S} سنة)')
                ax.set_title('محاكاة حية: دورة الحضارة بناءً على سؤالك')
                ax.legend(); ax.grid(True, alpha=0.3); ax.set_ylim(0, 1.05)
                st.pyplot(fig)
                
                # نصيحة إضافية
                if max_S < max_E and max_E - max_S > 20:
                    st.warning(f"⚠️ **تحذير استراتيجي:** فجوة الاستدراج = {max_E - max_S} سنة. الدولة ستبدو قوية لمدة {max_E - max_S} سنة رغم انهيار الإيمان. هذا هو 'الاستدراج'.")
            else:
                st.warning("🤔 لم أجد إجابة محددة. جرب أسئلة عن: المعادلة، الاستدراج، الحضارة، الفرد، الفيزياء، الاقتصاد، السياسة، التربية، التوبة.")
# =============================================
# الوحدة 2: لوحة القيادة الوطنية
# =============================================
elif module == "🌍 لوحة القيادة الوطنية":
    st.header("🌍 لوحة القيادة الوطنية – التوأم الرقمي")
    
    col1, col2 = st.columns(2)
    with col1:
        rule_of_law = st.slider("سيادة القانون", 0.0, 1.0, 0.5, 0.05)
        education = st.slider("التعليم والقيم", 0.0, 1.0, 0.5, 0.05)
        family = st.slider("الاستقرار الأسري", 0.0, 1.0, 0.5, 0.05)
    with col2:
        corruption = st.slider("مكافحة الفساد", 0.0, 1.0, 0.5, 0.05)
        justice = st.slider("العدل والمساواة", 0.0, 1.0, 0.5, 0.05)
        economic = st.slider("القوة الاقتصادية", 0.0, 1.0, 0.6, 0.05)
    
    W0 = (rule_of_law + education + family) / 3
    B0 = (corruption + justice + rule_of_law) / 3
    E0 = economic
    S0 = W0 * B0
    
    col1, col2, col3 = st.columns(3)
    col1.metric("W", f"{W0:.2f}"); col2.metric("B", f"{B0:.2f}"); col3.metric("S", f"{S0:.2f}")
    
    if st.button("شغّل المحاكاة", type="primary"):
        W_s, B_s, S_s, E_s = run_standard_simulation(W0, B0, E0, years=100)
        fig, axes = plt.subplots(1, 2, figsize=(12, 5))
        axes[0].plot(S_s, 'g-', label='S'); axes[0].plot(E_s, 'b--', label='E')
        axes[0].set_title('توقعات المستقبل'); axes[0].legend(); axes[0].grid(True, alpha=0.3)
        axes[1].scatter(B0, W0, s=400, c='red', edgecolors='black')
        axes[1].axhline(0.5, color='gray', ls=':'); axes[1].axvline(0.5, color='gray', ls=':')
        axes[1].set_xlim(0, 1); axes[1].set_ylim(0, 1)
        axes[1].set_xlabel('B'); axes[1].set_ylabel('W')
        axes[1].set_title('موقع الدولة الآن')
        plt.tight_layout(); st.pyplot(fig)

# =============================================
# الوحدة 3: مختبر المجتمع
# =============================================
elif module == "👥 مختبر المجتمع":
    st.header("👥 مختبر المجتمع – نموذج العوامل")
    
    pop_size = st.slider("عدد الأفراد", 100, 500, 300, 50)
    influence = st.slider("نصف قطر التأثير", 1.0, 4.0, 2.5, 0.5)
    
    if st.button("▶️ شغّل", type="primary"):
        W = np.random.uniform(0.3, 0.9, pop_size)
        B = np.random.uniform(0.3, 0.9, pop_size)
        px = np.random.randint(0, 30, pop_size)
        py = np.random.randint(0, 30, pop_size)
        
        for step in range(50):
            for i in range(pop_size):
                dist = np.sqrt((px - px[i])**2 + (py - py[i])**2)
                neighbors = np.where(dist < influence)[0]
                neighbors = neighbors[neighbors != i]
                if len(neighbors) > 0:
                    W[i] += 0.02 * (np.mean(W[neighbors]) - W[i])
                    B[i] += 0.02 * (np.mean(B[neighbors]) - B[i])
                W[i] = max(0.05, min(1.0, W[i] + 0.01*(np.random.rand()-0.5)))
                B[i] = max(0.05, min(1.0, B[i] + 0.01*(np.random.rand()-0.5)))
            px = np.clip(px + np.random.randint(-1, 2, pop_size), 0, 29)
            py = np.clip(py + np.random.randint(-1, 2, pop_size), 0, 29)
        
        saints = (W > 0.6) & (B > 0.6)
        hypos = (W < 0.4) & (B < 0.4)
        
        fig, ax = plt.subplots(figsize=(8, 8))
        ax.scatter(px[saints], py[saints], c='green', s=30, alpha=0.7, label='أتقياء')
        ax.scatter(px[hypos], py[hypos], c='red', s=30, alpha=0.7, label='منافقون')
        ax.scatter(px[~(saints|hypos)], py[~(saints|hypos)], c='gray', s=15, alpha=0.5)
        ax.set_title(f'خريطة المجتمع (بعد 50 سنة) – أتقياء: {np.sum(saints)} | منافقون: {np.sum(hypos)}')
        ax.legend(); ax.set_xlim(0, 29); ax.set_ylim(0, 29)
        st.pyplot(fig)

# =============================================
# الوحدة 4: صراع الحضارات
# =============================================
elif module == "⚔️ صراع الحضارات":
    st.header("⚔️ محاكاة صراع الحضارات")
    
    countries = [
        {"name": "دولة الإيمان", "W": 0.9, "B": 0.9, "E": 0.1, "color": "green", "hist_S": [], "hist_E": []},
        {"name": "دولة الترف", "W": 0.3, "B": 0.2, "E": 0.9, "color": "gold", "hist_S": [], "hist_E": []},
        {"name": "دولة النفاق", "W": 0.5, "B": 0.5, "E": 0.5, "color": "gray", "hist_S": [], "hist_E": []},
        {"name": "الإمبراطورية", "W": 0.1, "B": 0.9, "E": 0.85, "color": "red", "hist_S": [], "hist_E": []},
        {"name": "دولة العلم", "W": 0.8, "B": 0.6, "E": 0.4, "color": "cyan", "hist_S": [], "hist_E": []},
        {"name": "دولة التبعية", "W": 0.6, "B": 0.15, "E": 0.3, "color": "orange", "hist_S": [], "hist_E": []},
    ]
    
    if st.button("▶️ شغّل", type="primary"):
        years = 80
        for y in range(years):
            for c in countries:
                W, B, E = c['W'], c['B'], c['E']
                S = W * B; H = 10/(S+0.1)
                dW = 0.08*H - 0.05*E - 0.04*(1-B)
                dB = -0.04*E + 0.01*(1-B)*W*(1-W)
                W = max(0.01, min(1.0, W + dW))
                B = max(0.01, min(1.0, B + dB))
                E = max(0.01, min(1.0, E + 0.05*(S - E)))
                
                for o in countries:
                    if o['name'] == c['name']: continue
                    oS = o['W'] * o['B']
                    if abs(S - oS) > 0.5 and o['B'] > 0.7 and B < 0.4:
                        E -= 0.05; B -= 0.03
                    if o['E'] > 0.7 and B < 0.6:
                        B -= 0.02; W -= 0.01
                
                c['W'], c['B'], c['E'] = W, B, E
                c['hist_S'].append(W*B); c['hist_E'].append(E)
        
        fig, axes = plt.subplots(1, 2, figsize=(14, 6))
        for c in countries:
            axes[0].plot(c['hist_S'], color=c['color'], linewidth=2, label=c['name'])
            axes[1].plot(c['hist_E'], color=c['color'], linewidth=2, linestyle='--', label=c['name'])
        axes[0].set_title('منحنيات الثبات (S)'); axes[0].legend(fontsize=7); axes[0].grid(True, alpha=0.3)
        axes[1].set_title('منحنيات التمكين (E)'); axes[1].legend(fontsize=7); axes[1].grid(True, alpha=0.3)
        plt.tight_layout(); st.pyplot(fig)

st.markdown("---")
st.markdown("*قمرة القيادة المركزية – المنصة الكونية الكاملة v6.1*")
